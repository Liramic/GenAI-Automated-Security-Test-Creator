# Auto-generated from source imports on 2025-10-20
# Updated to include multi-cloud LLM support (Azure, Gemini, Claude, Ollama)

# Core LLM / LangChain stack
openai>=1.0.0
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
langchain-core>=0.2.0

# Environment management
python-dotenv>=1.0.0

# Validation / models
pydantic>=2.0.0

# Progress display
tqdm>=4.0.0

# HTML / formatting
json2html>=1.3.0

# Cryptography (AES via Crypto.Cipher)
pycryptodome>=3.18.0

# HTTP / networking
requests>=2.31.0
urllib3>=2.0.0

# Notes:
# - python-dotenv: Required for .env file loading (EnvConfig.py)
# - langchain-core: Required for langchain message types
# - pydantic v2: Required for structured output validation
# - urllib3: Used for SSL warning suppression in corporate proxy environments
# - All cloud LLM providers (Azure, Gemini, Claude) use requests library
# - Ollama support via langchain-community
# - Standard library & local modules intentionally excluded
